this
  .clearCommands()
  .do("move", "left")
  .do("move", "left")
  .do("run", 4)
  .do("move", "left")
  .do("run", 4)
  .do("move", "left")
  .do("run", 9)
  .do("move", "left")
  .do("run", 8)
  .do("shutdown")
  .start();