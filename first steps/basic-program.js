this
  .clearCommands()
  .do("turn", "left")
  .do("turn", "left")
  .do("run", 4)
  .do("turn", "left")
  .do("run", 4)
  .do("turn", "left")
  .do("run", 9)
  .do("turn", "left")
  .do("run", 8)
  .do("shutdown")
  .start();